/**
 * Supabase sync (optional). Copy this file to config.js and set url and anonKey
 * from Project Settings → API (Project URL and anon public key).
 *
 * If url or anonKey is empty, comments stay in chrome.storage.local only.
 *
 * Your display name and email are saved in the extension under Profile, not in this file.
 */
var DESIGN_REVIEW_SUPABASE = {
  url: 'https://YOUR_PROJECT_REF.supabase.co',
  anonKey: 'YOUR_SUPABASE_ANON_KEY',
};
